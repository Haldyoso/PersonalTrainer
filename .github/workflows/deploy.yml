name: Deploy na GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Beží vždy len jeden deploy; rozbehnutý sa nezruší, aby sa stránka nezasekla v polovici.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      # enablement: true zapne Pages priamo z workflowu, nech sa nemusí klikať v nastaveniach
      - uses: actions/configure-pages@v5
        with:
          enablement: true

      # Statická stránka bez buildu — len sa vyberie to, čo sa má publikovať.
      - name: Poskladať _site
        run: |
          mkdir -p _site
          cp index.html _site/
          cp -r assets _site/
          touch _site/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - id: deployment
        uses: actions/deploy-pages@v4
